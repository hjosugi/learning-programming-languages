# COBOL Payroll Lab

Batch-style payroll processing in COBOL.

## Run

```bash
./test.sh
```

The scripts expect a COBOL compiler such as GnuCOBOL (`cobc`). If it is not installed,
use this directory as a reading exercise and install the compiler before running the test.
